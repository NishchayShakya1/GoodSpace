import 'package:flutter/material.dart';

class Recruit extends StatelessWidget {
  const Recruit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Will Update this Soon"),
    );
  }
}
